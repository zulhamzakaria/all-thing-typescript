// assign the remaining elements to ...rest
let [a, b, ...rest] = [1, 2, 3, 4, 5];

function addNumbers(numbers: number[]) {
  //do somn
}

addNumbers([1]);
addNumbers([1, 2]);
addNumbers([1, 2, 3]);

function addNumbers2(...numbers: number[]) {
  //do somn
}

addNumbers2(1, 2);
