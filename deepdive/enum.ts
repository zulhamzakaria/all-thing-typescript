enum AccountType {
  savings,
  current = "Current",
}

// enum always shows the value
// 0
console.log(`Account type is ${AccountType.savings} 😃`);
// current
console.log(`Account type is ${AccountType.current}`);
